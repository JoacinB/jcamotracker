import 'package:flutter/material.dart';

class WeaponChallenges extends StatelessWidget {
  const WeaponChallenges({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Weapon Challenges'),
    );
  }
}